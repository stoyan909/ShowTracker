namespace ShowTracker.Common
{
    public static class HelperMethods
    {
        public static int GetSeasonDifference(int currentCount, int newCount)
        {
            return newCount - currentCount;
        }

    }
}
